prometheus:
  global:
    scrape_interval: 30s
  server:
    persistentVolume:
      enabled: true

grafana:
  adminPassword: "admin123"
  service:
    type: LoadBalancer
  sidecar:
    alerts:
      enabled: false  # Explicitly set it to 'false' if you don't need alerts
