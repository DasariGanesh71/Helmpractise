apiVersion: v1
kind: Service
metadata:
  name: grafana-service
  namespace: monitoring
spec:
  type: LoadBalancer
  loadBalancerIP: <EXISTING-LOADBALANCER-IP>
  selector:
    app: grafana
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000